apiVersion: v1
kind: Service
metadata:
  name: monitoring-grafana-nodeport
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/instance: monitoring
  ports:
  - name: http-web
    port: 3000
    targetPort: 3000
    nodePort: 30300
